<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="custom_sale_report_wizard_view_form" model="ir.ui.view">
        <field name="name">custom_sale_report_wizard_view_form</field>
        <field name="model">custom.sale.report.wizard</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <group>
                        <field name="start_date" />
                        <field name="end_date" />
                    </group>
                </sheet>
                <footer>
                    <button string="Generate Report" type="object" name="generate_report" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_sales_report" model="ir.actions.act_window">
        <field name="name">Sales Report</field>
        <field name="res_model">custom.sale.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="target">new</field>
        
    </record>

    <menuitem
        id="custom_sale_report_menu"
        name="Sales Report"
        action="action_sales_report"
        parent="account.account_reports_management_menu"
        groups="account.group_account_user"
        sequence="10"/>

</odoo>
